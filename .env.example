MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/rice_mill_book?retryWrites=true&w=majority
PORT=5000

# Any long random string. Used to sign login tokens - keep it secret, never commit the real one.
JWT_SECRET=change-this-to-a-long-random-string
